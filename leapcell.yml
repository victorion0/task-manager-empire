name: task-manager-empire
region: us-east-1
runtime: nodejs18

env:
  - key: DB_USER
    value: ${DB_USER}
  - key: DB_HOST
    value: ${DB_HOST}
  - key: DB_NAME
    value: ${DB_NAME}
  - key: DB_PASSWORD
    value: ${DB_PASSWORD}
  - key: DB_PORT
    value: ${DB_PORT}

entrypoint: server.js